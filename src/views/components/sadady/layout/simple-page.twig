<section class="sadady-simple-page">
  <div class="simple-page-card">
    <span class="cms-section-kicker">{{ page_kicker|default('سدادي') }}</span>
    <h1>{{ page_title|default('صفحة سدادي') }}</h1>
    <p>{{ page_description|default('هذه الصفحة جزء من ثيم سدادي داخل سلة، وتم تجهيزها كواجهة متوافقة مع صفحات Twilight القياسية.') }}</p>
    <a class="button primary" href="/">{{ page_cta|default('العودة للرئيسية') }}</a>
  </div>
</section>
